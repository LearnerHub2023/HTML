// (a+b)2 = a2+ b2 +2ab
// (a+b)^2 = a^2 +b^2 +2 * a * b

let expression;
let a = 0;
let b= 20;

expression = (a**2) + (b**2) + (2 * a * b); //arithmetic exp
 
expression = a < b; //boolean expression <, >, (<=, >=, ==), (===, <==, >==)

expression = a && b;   // ||, &&, !

// expression = a;
// let a;

console.log(expression);